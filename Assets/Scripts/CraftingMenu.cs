using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CraftingMenu : MonoBehaviour
{
    InventoryItem[][] contents;

    CraftingRecipe[] recipes;
    // TODO: i forgot the types for ui stuff, so define a crafting grid and output item slot here.


    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    InventoryItem getSlot(int x, int y)
    {

    }

    bool tryCraftRecipe()
    {
        // check recipes array for a matching recipe, make sure to check if each recipe is reversable or rotatable
    }

    void setSlot(int x, int y)
    {

    }
}
